---
- name: Install redis
  apt: pkg=redis-server state=present

- name: Make server accept redis connections on all hosts
  lineinfile: dest=/etc/redis/redis.conf regexp="^[\s]*bind[\s]*=" line="bind = 0.0.0.0"
  notify: restart redis-server